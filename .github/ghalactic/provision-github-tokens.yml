# yaml-language-server: $schema=https://ghalactic.github.io/provision-github-tokens/schema/requester.v1.schema.json

provision:
  secrets:
    RENOVATE_TOKEN:
      token: ./renovate.renovate
      github:
        repo:
          actions: true
